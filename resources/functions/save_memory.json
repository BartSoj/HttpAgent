{
  "type": "function",
  "function": {
    "name": "save_memory",
    "description": "save a memory for later use in the conversation",
    "parameters": {
      "type": "object",
      "properties": {
        "memory": {
          "type": "string",
          "description": "The memory to save. It should be a short string with something that can be used later in the conversation"
        }
      },
      "required": [
        "memory"
      ]
    }
  }
}